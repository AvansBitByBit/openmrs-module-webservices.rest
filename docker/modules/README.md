Generated OMOD files are copied here by the optional Docker Compose module-builder service.

Run it with:

```bash
docker compose -f docker-compose.dev.yml --profile build-module run --rm module-builder
```

Do not commit generated `.omod` files from this directory.
